spring.application.name=MIREA-FFJ-Practice-10

spring.jpa.database=postgresql
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://default_ip:5432/default_database_name}
spring.datasource.username=${POSTGRES_USER:default_username}
spring.datasource.password=${POSTGRES_PASSWORD:default_password}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update

jwt.secret=jhsAp9q28yr8pbcf2y2p9rcb2yrb39pb8c23ryub0893cru9
jwt.lifetime.access=30m
jwt.lifetime.refresh=30d

spring.redis.host=${REDIS_HOST}
spring.redis.port=${REDIS_PORT}
spring.redis.password=${REDIS_PASSWORD}